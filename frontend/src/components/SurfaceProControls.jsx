import React from 'react';\nimport { extend } from '@react-three/fiber';\nimport { OrbitControls } from 'three/examples/jsm/controls/OrbitControls';\nimport * as THREE from 'three';\n\n// Extend OrbitControls for use in R3F\nextend({ OrbitControls });\n\n/**\n * Enhanced OrbitControls optimized for Surface Pro trackpad\n * Provides better pan and zoom controls for 2-finger gestures\n */\nconst SurfaceProControls = React.forwardRef(({ target = [0, 1.5, 0], ...props }, ref) => {\n  return (\n    <orbitControls\n      ref={ref}\n      target={target}\n      // Enhanced pan settings for Surface Pro\n      enablePan={true}\n      panSpeed={3.0} // Increased sensitivity\n      screenSpacePanning={true} // Better for trackpad\n      \n      // Smooth zoom settings\n      enableZoom={true}\n      zoomSpeed={1.8} // More responsive zoom\n      \n      // Rotation settings\n      enableRotate={true}\n      rotateSpeed={1.2}\n      \n      // Smooth damping\n      enableDamping={true}\n      dampingFactor={0.08}\n      \n      // Distance limits\n      minDistance={0.3}\n      maxDistance={25}\n      \n      // Mouse button mapping\n      mouseButtons={{\n        LEFT: THREE.MOUSE.ROTATE,\n        MIDDLE: THREE.MOUSE.DOLLY,\n        RIGHT: THREE.MOUSE.PAN\n      }}\n      \n      // Touch controls optimized for Surface Pro\n      touches={{\n        ONE: THREE.TOUCH.ROTATE,\n        TWO: THREE.TOUCH.DOLLY_PAN // Two-finger pan and zoom\n      }}\n      \n      // Polar angle limits (prevent camera flip)\n      minPolarAngle={0}\n      maxPolarAngle={Math.PI}\n      \n      {...props}\n    />\n  );\n});\n\nSurfaceProControls.displayName = 'SurfaceProControls';\n\nexport default SurfaceProControls;